# RISC-V 矩阵扩展 32 位指令编码格式总结

## 1. 配置指令编码格式
`uop = 00`，`func3 = 000`，`major opcode = 0101011`。  
由 `inst[25]` 决定为立即数型或寄存器型，字段含义合并说明如下。

| 位域 | 位置 | 含义 | 详细说明 |
| :--- | :--- | :--- | :--- |
| `func4` | `[31:28]` | 配置类型 | `0000`：`msettilem`（M 方向）<br>`0001`：`msettilek`（K 方向）<br>`0010`：`msettilen`（N 方向）<br>当本条指令其余字段全为 0 时，表示 `mrelease` 指令 |
| `uop` | `[27:26]` | 微操作类型 | 固定为 `00` |
| `imm_sel` / `inst[25]` | `[25]` | 操作数来源 | `0`：立即数型，配置值来自 `imm10`<br>`1`：寄存器型，配置值来自 `rs1` |
| `imm10` | `[25:15]` (当 `inst[25]=0`) | 10 位立即数 | 新的 `mtilem` / `mtilek` / `mtilen` 值，直接写入对应 CSR |
| `rs1` | `[25:19]` (当 `inst[25]=1`) | 源寄存器 | 整数寄存器编号，其值将作为新的 tile 长度写入 CSR |
| `func3` | `[14:12]` | 功能码 | 固定为 `000` |
| `rd` | `[11:7]` | 目标寄存器 | 固定为 `00000`（x0），配置指令不写回通用寄存器 |
| `major opcode` | `[6:0]` | 主操作码 | `0101011` |

> **注意**：`mrelease` 指令的特例：`func4=0000`，`uop=00`，`inst[25]=0`，`imm10=0`，`func3=000`，`rd=00000`。执行后会将 `mstatus.MS` 设为 `01`（Initial）。

---

## 2. 加载/存储指令编码格式
`uop = 01`，`func3 = 000`，`major opcode = 0101011`。

| 位域 | 位置 | 含义 | 详细说明 |
| :--- | :--- | :--- | :--- |
| `func4` | `[31:28]` | 加载/存储类型 | 区分矩阵 A/B/C、是否转置、是否整寄存器操作。常见：<br>‑ 左矩阵 A 加载/存储（`mla*`/`msa*`）<br>‑ 右矩阵 B 加载/存储（`mlb*`/`msb*`）<br>‑ 输出矩阵 C 加载/存储（`mlc*`/`msc*`）<br>‑ 对应转置版本（`mlat*`/`msat*` 等）<br>‑ 整寄存器加载/存储（`mle*`/`mse*`，忽略 tile 形状）<br>具体编码由各助记符定义 |
| `uop` | `[27:26]` | 微操作类型 | 固定为 `01` |
| `ls` | `[25]` | 访问方向 | `0`：加载（Load）<br>`1`：存储（Store） |
| `rs2` | `[24:20]` | 行步幅寄存器 | 整数寄存器，存放**行步幅（字节）** |
| `rs1` | `[19:15]` | 基地址寄存器 | 整数寄存器，存放内存基地址 |
| `func3` | `[14:12]` | 功能码 | 固定为 `000` |
| `d_size` | `[11:10]` | 元素大小 | `00`：8 位<br>`01`：16 位<br>`10`：32 位<br>`11`：64 位 |
| `md` / `ms3` | `[9:7]` | 矩阵寄存器索引 | **加载**：目标矩阵寄存器（md）。**存储**：源矩阵寄存器（ms3）。<br>‑ A/B 矩阵操作：必须为 tile 寄存器（`000`–`011`）<br>‑ C 矩阵操作：必须为 acc 寄存器（`100`–`111`）<br>‑ 整寄存器加载/存储：二者均可 |
| `major opcode` | `[6:0]` | 主操作码 | `0101011` |

---

## 3. 矩阵乘法指令编码格式
`uop = 10`，`func3 = 000`，`major opcode = 0101011`。

| 位域 | 位置 | 含义 | 详细说明 |
| :--- | :--- | :--- | :--- |
| `func4` | `[31:28]` | 乘法类别 | `0000`：浮点矩阵乘法（含混合精度）<br>`0001`：整数矩阵乘法<br>`0010`：整数混合精度矩阵乘法 |
| `uop` | `[27:26]` | 微操作类型 | 固定为 `10` |
| `size_sup` | `[25:23]` | 扩展类型信息 | 由 `inst[25]`、`inst[24]`、`inst[23]` 三位共同决定操作数类型，详细子表见下方 |
| `ms2` | `[22:20]` | 源操作数 2（B 矩阵） | 必须是 tile 寄存器（`000`–`011`） |
| `s_size` | `[19:18]` | 源元素大小 | `00`：8 位<br>`01`：16 位<br>`10`：32 位<br>`11`：64 位 |
| `ms1` | `[17:15]` | 源操作数 1（A 矩阵） | 必须是 tile 寄存器（`000`–`011`） |
| `func3` | `[14:12]` | 功能码 | 固定为 `000` |
| `d_size` | `[11:10]` | 目的元素大小 | `00`：8 位（保留）<br>`01`：16 位<br>`10`：32 位<br>`11`：64 位 |
| `md` / `ms3` | `[9:7]` | 目的累加寄存器 | 必须是 acc 寄存器（`100`–`111`），用于保存 C 矩阵结果 |
| `major opcode` | `[6:0]` | 主操作码 | `0101011` |

### `size_sup` 字段详细含义子表

| `func4` | 位 | 说明 |
| :--- | :--- | :--- |
| **0000** (浮点) | `inst[25]` | 存在半精度浮点操作数时：<br>`0`：fp16<br>`1`：bf16 |
| | `inst[24]` | `0`：同精度乘法（如 `mfmacc.h`）<br>`1`：混合精度/扩宽乘法（如 `mfmacc.s.h`） |
| | `inst[23]` | 当源为 fp8（E4M3/E5M2）时：<br>`0`：E5M2<br>`1`：E4M3<br>当 `inst[24]=1` 且需要指示第二源符号性时：<br>`0`：ms2 无符号<br>`1`：ms2 有符号 |
| **0001** (整数) | `inst[25]` | 当 `s_size=00` 时：<br>`0`：源为 int8<br>`1`：源为 int4 |
| | `inst[24]` | `0`：ms1 无符号<br>`1`：ms1 有符号 |
| | `inst[23]` | `0`：ms2 无符号<br>`1`：ms2 有符号 |
| **0010** (整数混合) | `inst[24]` | `0`：ms1 无符号<br>`1`：ms1 有符号 |
| | `inst[23]` | `0`：ms2 无符号<br>`1`：ms2 有符号 |

> **指令示例**  
> - `mmacc.w.b`：`func4=0001`, `s_size=00`, `d_size=10`, `inst[25]=0`, `inst[24]=1`, `inst[23]=1`（有符号 int8 乘累加至 int32）  
> - `mfmacc.h`：`func4=0000`, `s_size=01`, `d_size=01`, `inst[24]=0`（fp16 同精度）  
> - `mfmacc.s.h`：`func4=0000`, `s_size=01`, `d_size=10`, `inst[24]=1`, `inst[25]=0`（fp16→fp32 双宽乘加）


- 使用 custom-1（0101011）主操作码，分为 3 种格式
  - 纯矩阵寄存器操作（md/ms1/ms2/d_size/s_size/func4/uop）
  - 带 GPR 操作数（rd/rs1/rs2）
  - 带 10-bit 立即数（用于配置指令）
uop 字段区分 5 类指令：
- 00：配置指令
- 01：Load/Store
- 10：矩阵乘法
- 11：MISC / Element-wise（通过 func3 进一步区分）

md/ms1/ms2[2:0] Matrix Register Index
000 tr0
001 tr1
010 tr2
011 tr3
100 acc0
101 acc1

AME Tile Register / Accumulator → CUTE Scratchpad 映射:
tr0  ─→  ASCP[0] (A matrix, bank 0) │ tr1  ─→  ASCP[1] (A matrix, bank 1) 
tr2  ─→  BSCP[0] (B matrix, bank 0) │ tr3  ─→  BSCP[1] (B matrix, bank 1) 
acc0 ─→  CSCP[0] (C/D accumulator, bank 0) │acc1 ─→  CSCP[1] (C/D accumulator, bank 1)  
